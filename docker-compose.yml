services:
  db:
    image: postgres:18-alpine
    container_name: flyhigh_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: FlyHighDb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - flyhigh_network

  adminer:
    image: adminer
    container_name: flyhigh_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      - db
    networks:
      - flyhigh_network

  backend:
    build:
      context: ./flyhigh-cs
      dockerfile: Dockerfile
    container_name: flyhigh_backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - ConnectionStrings__PostgresConnection=Host=db;Port=5432;Database=FlyHighDb;Username=postgres;Password=${DB_PASSWORD}
      - ASPNETCORE_ENVIRONMENT=Production
      - Jwt__Key=${JWT_SECRET}
      - EmailSettings__Password=${EMAIL_PASSWORD}
    depends_on:
      - db
    networks:
      - flyhigh_network

  frontend:
    build:
      context: ./flyhigh-nt
      dockerfile: Dockerfile
    container_name: flyhigh_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=https://flyhigh-volleyball.cz/api
      - NODE_OPTIONS=--dns-result-order=ipv4first
    depends_on:
      - backend
    networks:
      - flyhigh_network

volumes:
  pgdata:


networks:
  flyhigh_network:
    driver: bridge
